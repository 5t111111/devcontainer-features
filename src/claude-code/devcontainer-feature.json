{
    "id": "claude-code",
    "version": "0.3.1",
    "name": "Claude Code",
    "documentationURL": "https://github.com/5t111111/devcontainer-features/tree/main/src/claude-code",
    "description": "Installs the native Claude Code CLI with checksum verification and persists user configuration including authentication state across container rebuilds.",
    "options": {
        "version": {
            "type": "string",
            "enum": [
                "latest",
                "stable"
            ],
            "default": "latest",
            "description": "Version channel to install (latest or stable)"
        },
        "persistUserConfig": {
            "type": "boolean",
            "default": true,
            "description": "Persist Claude Code user configuration (.claude directory) using named volume. Set to false to disable persistence for security-sensitive environments."
        }
    },
    "mounts": [
        {
            "type": "volume",
            "source": "claude-config-${devcontainerId}",
            "target": "/var/lib/claude-config"
        }
    ],
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
